import Activity from '@/components/Camp/Activity';
import Section from '@/layout/Section';

const ActivitySection = () => {
  return (
    <Section id='#activity' variant='mt-20 lg:mt-52 mb-20'>
      <Activity />
    </Section>
  );
};

export default ActivitySection;
